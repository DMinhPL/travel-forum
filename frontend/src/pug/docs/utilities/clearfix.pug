extends ../../layouts/_layout_doc 
block setting
    -var mainActive = 'docs';
    -var slideActive = 'clearfix';
    - title = "Utilities - Clearfix"

block content 
    h1.bd-title#content Clearfix
    p.bd-lead Quickly and easily clear floated content within a container by adding a clearfix utility.

    p
        | Easily clear 
        code.highlighter-rouge float
        | s by adding 
        code.highlighter-rouge .clearfix
        strong to the parent element
        | . Can also be used as a mixin.

    figure.highlight
        pre
            code.css
                | .clearfix

    figure.highlight
        pre
            code.less
                | // Mixin itself
                | @mixin clearfix() {
                |     &::after {
                |         display: block;
                |         content: "";
                |         clear: both;
                |     }
                | }

                | // Usage as a mixin
                | .element {
                |     @include clearfix;
                | }
    
    p The following example shows how the clearfix can be used. Without the clearfix the wrapping div would not span around the buttons which would cause a broken layout.

    .bd-example
        .bg-info.clearfix
            button.btn.btn-secondary.float-left(type='button') Example Button floated left
            button.btn.btn-secondary.float-right(type='button') Example Button floated right

    figure.highlight
        pre
            code.css
                | .bg-info.clearfix
                |     button.btn.btn-secondary.float-left(type='button') Example Button floated left
                |     button.btn.btn-secondary.float-right(type='button') Example Button floated right